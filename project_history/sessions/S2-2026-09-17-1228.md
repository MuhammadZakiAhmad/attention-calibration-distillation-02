# Session S2-2026-09-17-1228

**Session Status**: ACTIVE
**Resumed from**: S1-2026-09-17-1148
**Working on**: Pre-Phase-1 hardening. Pinned `model.revision`, froze `distill_layers`, resolved the Phase 2 design questions that gated the Teacher hidden-state cache (GQA strategy, attachment point, loss targets). Next: git init + push, then the Colab GPU generation run that produces the Golden Records.
**Starting point**: Resumed from S1-2026-09-17-1148. Phase 1 built and verified locally on CPU; Colab GPU generation run never executed.

**What is working**:
- `python phase1.py --stage prepare` runs clean on CPU. Produces a 500-target pool (250 True / 250 False, 494 unique theories) and the two fixed demonstrations. Reproducible from `generation.seed`.
- ProofWriter proof renderer: 25,616 questions rendered across the full depth-2 train split with **zero** parse failures.
- `parse_answer` (9 cases) and `classify` (4 outcomes) unit-tested, 0 failures.
- Chat-template consistency verified: the `generate()` tokenization path is token-identical to `apply_chat_template(tokenize=True)` (163 tokens both ways). This is the spec §10 failure mode that broke the previous project.
- Leakage/symmetry invariants verified across all 500 targets: Student prompt is a verbatim suffix of the Teacher prompt; target gold proof absent from Teacher prompt; demo context absent from Student prompt. Zero violations on all three.
- `--stage classify` exercised end-to-end on synthetic generations; produced all six spec §41 artifacts, idempotent across repeat runs.
- Resume verified: with 500/500 already generated, `--stage generate` reports `0 to do` and skips model loading entirely.
- `$ACD_ROOT` artifact-root indirection works — the same code path serves local and Colab runs.
- Pool integrity confirmed: lines containing "true"/"false" are 250/250; distinct `gold_answer` values are exactly `{'True', 'False'}`; lowercase `true` appears 0 times, so no Python bools are serialized.
- Qwen2.5-3B-Instruct confirmed to have 36 transformer layers, so `training.target_layers: [32,33,34,35]` correctly names the final four.

**What is broken**: Nothing known. The GPU generation path (`--stage generate` with a real model) has **not** been executed — no local CUDA. Its components are tested piecewise (prompt building, chat rendering, batching shape, append-only writer) but the end-to-end Colab run is unproven. This is the single remaining blocker for Phase 1.

**Files changed**: —

**Critical commands / config / snippets**:
- `python phase1.py --stage prepare` — CPU only, builds pool + demos. Safe to re-run.
- `python phase1.py --stage all` — full run, needs GPU.
- Colab: `export ACD_ROOT=/content/drive/MyDrive/acd02` then the same commands.
- Resume flags: `--stage generate` (only missing), `--stage classify` (rebuild from cache), `--limit N`, `--conditions zero_shot`, `--batch-size`.
- Golden Record definition: `gold_answer == few_shot_answer AND gold_answer != zero_shot_answer`.
- Label mapping is binary only: `True` / `False`. Parse failure → `INVALID` → counts as incorrect → **do not rerun** (spec §9).
- Teacher prompt = demo blocks + `\n\nTarget\n\n` + `target_block(...)`. Student prompt = `target_block(...)` verbatim. Deliberately structured so the Student prompt is a literal suffix of the Teacher prompt — this both enforces spec §10 and gives Phase 2's LCS alignment a clean anchor.

**Environment facts**: Windows 11, Git Bash, local VS Code authoring. Local Python 3.12.4, `datasets` 4.8.5, `transformers` 4.47.0, `torch` 2.5.1+**cpu** (no CUDA) — local runs can do everything except 3B inference. Compute happens in Colab; Drive persists artifacts; Colab local FS is temporary (spec §54). Datasets download fine locally. Note: Git Bash `/tmp` does not map to Python's `C:\tmp` on Windows — use a Windows-native path for any throwaway `ACD_ROOT`.

**Dependencies added**: —

**Assumptions (unverified)**:
- Greedy decoding with `max_new_tokens=512` is enough for depth-2 proof chains. If the Colab run shows a high `INVALID` rate, that is the first thing to check — but note that changing it invalidates the pool and requires a full Phase 1 re-run.
- Colab GPU can hold Qwen2.5-3B-Instruct for inference. Historical OOM was from running Teacher and Student *simultaneously*; Phase 1 runs them sequentially by construction, so that specific failure should not recur.
- The two fixed demonstrations transfer useful behaviour at all. If Phase 1 yields very few Golden Records, that assumption is what failed — check `both_failed` counts first.

**External references**: `HyperICL.pdf` (architectural source of truth), `Attention Calibration Distillation — Three-Phase Experimental Project Specification.md` (the contract), HuggingFace dataset `arqa39/proofwriter-source`.

**Open questions / blockers**:
1. **Not run yet**: the Colab GPU generation stage. Everything downstream depends on it. This is the only hard blocker remaining for Phase 1.
2. `training.epochs: 3`, `ce_weight: 1.0`, `mse_weight: 0.5` are unvalidated starting values carried over from the historical config. Spec §25 requires they be fixed and recorded, but the right values are unknown until a first Phase 2 run.
3. Phase 2 module wiring is now specified (post-RoPE, scaled Q, zero-init — see Decisions Made) but not yet implemented; `phase2.py` does not exist.
4. Not yet pushed to GitHub. Spec §54 wants GitHub as source of truth.

**Resolved this session**:
- GQA 16:2 head ratio — no longer a design question. `Δ_q = Q_q Uq Ukᵀ K_gᵀ` for `q` in KV-group `g`, so repeating the rank-32 result across the group is arithmetically identical to any grouped formulation. Spec §23's formula applies verbatim. Model stays Qwen2.5-3B-Instruct.
- `model.revision` pinned to `aa8e72537993ba99e69dfaafa59ed015b17504d1`.
- `distill_layers` frozen to `[32, 33, 34, 35]` (= `target_layers`), so exactly four layers get cached.
- Attachment point fixed: post-RoPE, into raw attention scores, `Uq` consuming scaled Q, `Uq`/`Uk` zero-initialised, `attn_implementation: eager`.
- Loss targets fixed: `L_CE` over the Teacher's full generated output; `L_MSE` over target-block positions only.

**Next steps**:
1. ~~Pin `model.revision` and freeze `distill_layers`.~~ Done.
2. ~~Resolve GQA / attachment / loss-target questions.~~ Done.
3. `git init`, commit, push to GitHub (spec §54). Note `.gitignore` excludes `results/`, `data/phase1/`, `*.jsonl`, checkpoints, and `HyperICL.pdf` — only code, configs, and the two committed data files travel.
4. Clone the repo into the Google Drive folder that will serve as `ACD_ROOT`, so code and artifacts persist together across Colab sessions.
5. In Colab: mount Drive, `export ACD_ROOT=<the Drive folder>`, `pip install -r requirements.txt` (peak: uninstall Colab's preinstalled torch if the CUDA build mismatches), then `python phase1.py --stage all`. Expect the model download to dominate the first run.
6. Inspect `results/phase1/phase1_config.json` category counts. **Key risk signal: a low Golden count.** If Golden is small, check `both_failed` first — that means the 2-shot Teacher is not solving either, which points at the demonstration or prompt quality rather than the technique. Also check `invalid_counts`; a high `INVALID` rate means `max_new_tokens=512` is truncating.
7. Only after Phase 1 data exists: write `phase2.py` against the frozen design.

**Lessons learned**: —

**Outcome**: —
